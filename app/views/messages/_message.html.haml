.chat__message{ "message-id": "#{message.id}" }
  %li.user-name
    %p= message.user.name
  %li.send-time
    %p= message.created_at
  %li.send-message
    - if message.body.present?
      %p= message.body
    = image_tag message.image, class: 'lower-message__image' if message.image.present?
